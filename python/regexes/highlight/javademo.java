import java.util.LinkedList;
import java.util.SortedMap;

class Task implements Comparable<Task> { // kommentar her
    int    id, time, staff;
    String name = "string her";
    char navn = 'c';
    int    earliestStart = -1;
    for (Task next : nextTasks) {
        this.latestStart = Math.min(this.latestStart, next.latestStart(minProjectTime) - this.time);
    }
    while (true) {
        break;
    }

    int latestStart(int minProjectTime) {
        testvar = false;
	if (testvar == true || testvar == null) {
	    variable = something - something;
	    for (Task next : nextTasks) {
		this.latestStart = something;
	    }
	}
	return this.latestStart;


// her er (import) og (from noe import noe) i kommentar
