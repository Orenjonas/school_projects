import noe_greier as ng
from noe_annet import noe_spesifikt

def min_func(x, y): # Dette er en funksjon som gjør noe
    s = "streng med dobbelparentes"
    s2 = 'treng med enkeltparentes'
    a = 2
    return a**2

for i in range(10):
    bool_var = True

    if (bool_var == False or bool_var == None):
        # gjør noe greier

    elif:
        # gjør noe annet

    else:
        # Alt annet

    while (True):
        break

# her er (import) og (from noe import noe) i kommentar
